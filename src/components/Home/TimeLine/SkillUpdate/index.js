import React from 'react'

import Card from '../../../UI/Card'
import Label from '../../../UI/Label'

import classes from './SkillUpdate.module.css'

const SkillUpdate = () => (
    <div className = { `${ classes.Container }`}>
        <Card>
            <Label>

            </Label>
        </Card>
    </div>
)

export default SkillUpdate

